{
  "name": "grimsoul-wiki",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/validate-data.mjs && node scripts/check-icons.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "node scripts/validate-data.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "zod": "^3.23.0"
  }
}
